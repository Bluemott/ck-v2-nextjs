version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting development build process for Cowboy Kimono REST API"
        - echo "Node version: $(node --version)"
        - echo "npm version: $(npm --version)"
        - echo "Setting development environment variables"
        - 'export NODE_ENV="production"'
        - 'export NEXT_PUBLIC_USE_REST_API="true"'
        - 'export NEXT_PUBLIC_SITE_URL="https://dev-amplify-deployment.d1crrnsi5h4ht1.amplifyapp.com"'
        - 'export NEXT_PUBLIC_WPGRAPHQL_URL="https://api.cowboykimono.com"'
        - 'export NEXT_PUBLIC_WORDPRESS_ADMIN_URL="https://admin.cowboykimono.com"'
        - echo "Installing dependencies"
        - npm ci --legacy-peer-deps --no-optional
        - echo "Dependencies installed successfully"
    build:
      commands:
        - echo "Starting development build phase"
        - 'export NODE_ENV="production"'
        - 'export NEXT_PUBLIC_USE_REST_API="true"'
        - 'export NEXT_PUBLIC_SITE_URL="https://dev-amplify-deployment.d1crrnsi5h4ht1.amplifyapp.com"'
        - 'export NEXT_PUBLIC_WPGRAPHQL_URL="https://api.cowboykimono.com"'
        - echo "Building Next.js application for development"
        - npm run build
        - echo "Development build completed successfully"
        - echo "Build artifacts:"
        - ls -la .next
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/* 