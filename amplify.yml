version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🚀 Starting Simplified Build Process"
        - echo "Node version: $(node --version)"
        - echo "npm version: $(npm --version)"
        - echo "Setting build environment variables..."
        # Set safe defaults for build time
        - export NODE_ENV=production
        - export NEXT_PUBLIC_USE_AWS_GRAPHQL=false
        - export NEXT_PUBLIC_SITE_URL=https://cowboykimono.com
        - echo "Installing dependencies with simplified approach..."
        - npm ci --legacy-peer-deps --no-optional
        - echo "✅ Dependencies installed successfully"
    build:
      commands:
        - echo "🔨 Starting build phase"
        - echo "Setting production environment..."
        - export NODE_ENV=production
        - export NEXT_PUBLIC_USE_AWS_GRAPHQL=false
        - npm run build
        - echo "✅ Build completed successfully"
        - echo "📁 Build artifacts:"
        - ls -la .next
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/* 