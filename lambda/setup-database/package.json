{
  "name": "wordpress-database-setup",
  "version": "1.0.0",
  "description": "Lambda function for setting up WordPress database schema",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "package": "zip -r ../setup-database.zip ."
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}